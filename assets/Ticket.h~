#ifndef assets_Ticket_h
#define assets_Ticket_h

//library headers
//local headers

class Ticket{
public:
	char* eventName;
	int quantity;
	int price;
	char* username;
	
	Ticket( char* line);
	Ticket( char* eventname, int quantity, int price,
		char* username);
	
	bool isEnd();
public:
};

#endif
